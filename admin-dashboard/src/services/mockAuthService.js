// Este archivo ha sido limpiado para eliminar todos los datos de ejemplo y demo

/**
 * Función vacía que reemplaza el servicio de autenticación simulado
 * En un entorno de producción, esta función no hace nada ya que se utilizan
 * los servicios reales de autenticación.
 */
export function initMockAuthService() {
  console.log('Servicio de autenticación mock deshabilitado - usando servicios reales');
}

// Todas las funciones de mock han sido deshabilitadas
// En producción se utilizan los servicios reales de la API